app:
  app_name: "repo-stat-api"

http:
  address: ":8080"
  timeout: "5s"

logger:
  log_level: "DEBUG"

services:
  subscriber: "subscriber:8080"
  processor: "processor:8082"
  collector: "collector:8083"