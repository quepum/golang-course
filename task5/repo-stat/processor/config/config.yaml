app:
  app_name: "repo-stat-processor"

grpc:
  address: ":8082"
  timeout: "5s"

logger:
  log_level: "DEBUG"

services:
  collector: "collector:8083"

kafka:
  broker: "kafka:9092"
  topic_request: "repo-fetch-request"
  topic_result: "repo-fetch-result"